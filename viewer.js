const db=supabase.createClient(window.SUPABASE_URL,window.SUPABASE_KEY);
const esc=s=>String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
const status=(s)=>s==="online"?'<span class="status hunting"><i class="dot"></i>UPANDO</span>':s==="pz"?'<span class="status pz"><i class="dot"></i>PZ</span>':'<span class="status offline"><i class="dot"></i>OFFLINE</span>';
async function load(){const q=(document.getElementById("search").value||"").toLowerCase();const {data,error}=await db.from("players").select("*").order("level",{ascending:false});if(error){document.getElementById("body").innerHTML=`<tr><td colspan="5">Configure o Supabase para carregar os jogadores.</td></tr>`;return}const online=data.filter(x=>x.status!=="offline");document.getElementById("online").textContent=online.length;document.getElementById("soul").textContent=online.filter(x=>x.group==="soul").length;document.getElementById("hunted").textContent=online.filter(x=>x.group==="hunted").length;const list=online.filter(x=>x.name.toLowerCase().includes(q));document.getElementById("body").innerHTML=list.map(x=>`<tr><td><b>${esc(x.name)}</b></td><td>${x.group==="soul"?"SOUL TATICS":"HUNTED"}</td><td>${x.level}</td><td>${status(x.status)}</td><td>${x.last_seen?new Date(x.last_seen).toLocaleString("pt-BR"):"—"}</td></tr>`).join("")||'<tr><td colspan="5">Nenhum jogador online.</td></tr>';document.getElementById("updated").textContent="Atualizado "+new Date().toLocaleTimeString("pt-BR");document.getElementById("scan").textContent="Atualizado "+new Date().toLocaleTimeString("pt-BR")}
document.getElementById("search").oninput=load;document.getElementById("refresh").onclick=load;setInterval(()=>document.getElementById("clock").textContent=new Date().toLocaleTimeString("pt-BR"),1000);load();setInterval(load,30000);