const db=supabase.createClient(window.SUPABASE_URL,window.SUPABASE_KEY);const group=location.pathname.includes("hunteds")?"hunted":"soul";const esc=s=>String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));function st(s){return s==="online"?'<span class="status hunting"><i class="dot"></i>UPANDO</span>':s==="pz"?'<span class="status pz"><i class="dot"></i>PZ</span>':'<span class="status offline"><i class="dot"></i>OFFLINE</span>'}async function load(){const {data}=await db.from("players").select("*").eq("group",group).order("level",{ascending:false});const q=(document.getElementById("search").value||"").toLowerCase();document.getElementById("grid").innerHTML=(data||[]).filter(x=>x.name.toLowerCase().includes(q)).map(x=>`<div class="char"><div class="char-top"><div class="char-name">${esc(x.name)}</div><div class="char-level">LV ${x.level}</div></div><div style="margin-top:14px">${st(x.status)}</div><div class="char-meta"><span>${group==="soul"?"SOUL TATICS":"HUNTED"}</span><span>${x.last_seen?new Date(x.last_seen).toLocaleString("pt-BR"):"—"}</span></div></div>`).join("")||'<div class="char">Nenhum personagem cadastrado.</div>'}document.getElementById("search").oninput=load;load();setInterval(load,30000);